// REACT
import React from "react";

// STYLED COMPONENTS
import { LayoutName } from "./name.styles";

const Name = ({ children }) => {
  return <LayoutName>{children}</LayoutName>;
};

export default Name;
