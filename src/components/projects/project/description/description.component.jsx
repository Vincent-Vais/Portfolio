import React from "react";

import { LayoutDescription } from "./description.styles";
const Description = ({ children }) => (
  <LayoutDescription>{children}</LayoutDescription>
);

export default Description;
